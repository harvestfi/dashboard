import React from 'react'
import { observer } from 'mobx-react'
import { useStores } from '@/stores/utils'

export const FarmingTable = observer((props) => {
  const {} = useStores()

  return <></>
})
